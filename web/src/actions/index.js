// @flow
// @ts-check

import { login, register } from './auth';
import { getMessages, sendMessage } from './messages';
import { addFriend, getFriends } from './friends';

export { login, register, getMessages, sendMessage, addFriend, getFriends };
