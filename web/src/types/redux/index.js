// @flow

type ExampleType = {
    foo: number,
    bar: boolean,
    baz: string,
};

type AuthType = {
    authenticated: boolean,
};

// eslint-disable-next-line
export type { ExampleType, AuthType };
